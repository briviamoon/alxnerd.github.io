#include "stdio.h"

/**
 * main - prints C is Fun
 * Return: 0 when successful
*/

int main(void)
{
	char *name = "Alx Student";
	int betty = 1;
	int kimba = 1;
	int nice = 2;

	if ((betty + kimba) == nice)
	{
		printf("I'm a %s\n", name);
	}
	return (0);
}
